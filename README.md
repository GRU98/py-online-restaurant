# Шепіт Мандрагори – Online Restaurant

"Шепіт Мандрагори" – це чарівний онлайн-ресторан із зимовою атмосферою Гоґвортсу. Проєкт створений на Flask із використанням PostgreSQL, забезпечує реєстрацію й авторизацію гостей, адміністрування меню та обробку замовлень і бронювань.

## Основні можливості

- 🌨️ Зимова тематика Гоґвортсу в HTML/CSS оформленні
- 👥 Реєстрація та вхід користувачів із безпечним хешуванням паролю (bcrypt)
- 🧙‍♂️ Адмін-кабінет із обліковими даними `Admin / 123456789`
- 📜 Керування сезонним меню, перегляд позицій та кошик
- 🛒 Оформлення та перегляд власних замовлень
- 🕯️ Бронювання столиків із перевіркою наявності місць
- 🛡️ Захист від CSRF, обмеження на розмір файлів і базові заголовки безпеки

## Технології

- Python 3.13+
- Flask
- SQLAlchemy + PostgreSQL
- Flask-Login
- bcrypt

## Швидкий старт

1. **Клонувати репозиторій та перейти** до каталогу проєкту:
   ```bash
   git clone <repo-url>
   cd PythonProject12
   ```
2. **Створити та активувати віртуальне середовище** (рекомендується):
   ```bash
   python -m venv .venv
   # Windows PowerShell
   .venv\Scripts\Activate.ps1
   ```
3. **Встановити залежності**:
   ```bash
   pip install -r requirements.txt
   ```
4. **Налаштувати змінні середовища** для підключення до PostgreSQL:
   ```bash
   set PGUSER=<your user name>
   set PGPASSWORD=<your password>
   set PGHOST=<your host>
   set PGPORT=<your port>
   set PGDATABASE=<your database name>
   ```

5. **Ініціалізувати базу даних та запустити сервер**:
   ```bash
   python online_restaurant.py
   ```
   Після першого запуску створиться адмін-користувач і сезонне меню.

6. **Відкрити застосунок** у браузері: <http://127.0.0.1:5000/>

## Налаштування

- **Адмін-акаунт**: автоматично створюється при старті (логін `Admin`, пароль `123456789`).
- **Каталог завантажень**: `static/menu/` – зберігає ілюстрації для позицій меню.
- **CSRF токен** генерується автоматично та передається у формах.

## Корисні команди для розробки

```bash
# Форматування / статичний аналіз (якщо додати відповідні інструменти)
ruff check .

# Оновлення залежностей
pip freeze > requirements.txt
```

## Структура каталогу

```
PythonProject12/
├─ online_restaurant.py        # Flask-додаток
├─ online_restaurant_db.py     # Моделі SQLAlchemy
├─ requirements.txt            # Залежності
├─ static/                     # Статичні файли (зображення меню тощо)
└─ templates/                  # HTML-шаблони (Jinja2)
```

## Ліцензія

Проєкт створено для навчальних цілей. Ви можете адаптувати та розширювати його під власні потреби.
